<h3>Student List<a href="/student" class="btn btn-secondary" style="float:right">Create New</a></h3>
<div class="table-responsive">
  <table class="table table-dark table-hover container-fluid">
    <thead>
      <tr>
        <th>Full Name</th>
        <th>Email</th>
        <th>Mobile</th>
        <th>City</th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {{#each list}}
      <tr>
        <td>{{this.fullName}}</td>
        <td>{{this.email}}</td>
        <td>{{this.mobile}}</td>
        <td>{{this.city}}</td>
        <td>
            <a href="/student/{{this.id}}" class="btn btn-primary btn-sm" style="background:#17A2B8">Edit</a>
        </td>
        <td>
          <a href="/student/delete/{{this.id}}" class="btn btn-primary btn-sm" style="background:#17A2B8"
          onclick="return confirm('Are you sure you want to delete this record?');">Delete
        </i>
        </a>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
</div>
